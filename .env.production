# Production environment settings
NEXTAUTH_URL="https://your-production-url.vercel.app"
# Don't include actual database credentials here, use deployment platform environment variables instead
# DATABASE_URL will be set through the hosting platform
